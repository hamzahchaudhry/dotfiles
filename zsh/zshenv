# ==========================================
# zsh bootstrap
# ==========================================
# sets ZDOTDIR so zsh loads its config from
# $HOME/.config/zsh instead of $HOME.
# meant to be installed into /etc/zsh/zshenv so it
# applies to all zsh invocations.

export ZDOTDIR="${ZDOTDIR:-$HOME/.config/zsh}"
